{% extends 'base.njk' %}

{% block body %}
  
  {{ content | safe }}

  <table class="table table-striped">
    <thead>
      <tr><th>#</th><th>Predictor</th><th>Score</th></tr>
    <thead>
    <tbody>
      {% for p in predictors %}
        <tr><td>{{ loop.index }}</td><td>{{ p.name }}</td><td>{{ p.currentScore }}</td></tr>
      {% endfor %}
    </tbody>
  </table>
  
{% endblock %}